// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: cs_prediction_events.proto

/* eslint-disable */
import { BinaryReader } from "@bufbuild/protobuf/wire";
import { CMsgQAngle, CMsgVector } from "./networkbasetypes.js";

export const protobufPackage = "";

export const ECSPredictionEvents = { CSPE_DamageTag: 1, CSPE_PlayerTeleport: 3, UNRECOGNIZED: -1 } as const;

export type ECSPredictionEvents = typeof ECSPredictionEvents[keyof typeof ECSPredictionEvents];

export namespace ECSPredictionEvents {
  export type CSPE_DamageTag = typeof ECSPredictionEvents.CSPE_DamageTag;
  export type CSPE_PlayerTeleport = typeof ECSPredictionEvents.CSPE_PlayerTeleport;
  export type UNRECOGNIZED = typeof ECSPredictionEvents.UNRECOGNIZED;
}

export interface CCSPredictionEvent_DamageTag {
  flinch_mod_small?: number | undefined;
  flinch_mod_large?: number | undefined;
  friendly_fire_damage_reduction_ratio?: number | undefined;
}

export interface CCSPredictionEvent_PlayerTeleport {
  relative?: boolean | undefined;
  origin?: CMsgVector | undefined;
  angles?: CMsgQAngle | undefined;
  velocity?: CMsgVector | undefined;
}

function createBaseCCSPredictionEvent_DamageTag(): CCSPredictionEvent_DamageTag {
  return { flinch_mod_small: undefined, flinch_mod_large: undefined, friendly_fire_damage_reduction_ratio: undefined };
}

export const CCSPredictionEvent_DamageTag: MessageFns<CCSPredictionEvent_DamageTag> = {
  decode(input: BinaryReader | Uint8Array, length?: number): CCSPredictionEvent_DamageTag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCCSPredictionEvent_DamageTag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.flinch_mod_small = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.flinch_mod_large = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.friendly_fire_damage_reduction_ratio = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseCCSPredictionEvent_PlayerTeleport(): CCSPredictionEvent_PlayerTeleport {
  return { relative: undefined, origin: undefined, angles: undefined, velocity: undefined };
}

export const CCSPredictionEvent_PlayerTeleport: MessageFns<CCSPredictionEvent_PlayerTeleport> = {
  decode(input: BinaryReader | Uint8Array, length?: number): CCSPredictionEvent_PlayerTeleport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCCSPredictionEvent_PlayerTeleport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.relative = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.origin = CMsgVector.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.angles = CMsgQAngle.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.velocity = CMsgVector.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
