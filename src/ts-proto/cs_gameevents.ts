// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.1
// source: cs_gameevents.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { CMsgQAngle, CMsgVector } from "./networkbasetypes.js";

export const protobufPackage = "";

export const ECsgoGameEvents = {
  GE_PlayerAnimEventId: 450,
  GE_RadioIconEventId: 451,
  GE_FireBulletsId: 452,
  GE_PlayerBulletHitId: 453,
  UNRECOGNIZED: -1,
} as const;

export type ECsgoGameEvents = typeof ECsgoGameEvents[keyof typeof ECsgoGameEvents];

export namespace ECsgoGameEvents {
  export type GE_PlayerAnimEventId = typeof ECsgoGameEvents.GE_PlayerAnimEventId;
  export type GE_RadioIconEventId = typeof ECsgoGameEvents.GE_RadioIconEventId;
  export type GE_FireBulletsId = typeof ECsgoGameEvents.GE_FireBulletsId;
  export type GE_PlayerBulletHitId = typeof ECsgoGameEvents.GE_PlayerBulletHitId;
  export type UNRECOGNIZED = typeof ECsgoGameEvents.UNRECOGNIZED;
}

export function eCsgoGameEventsFromJSON(object: any): ECsgoGameEvents {
  switch (object) {
    case 450:
    case "GE_PlayerAnimEventId":
      return ECsgoGameEvents.GE_PlayerAnimEventId;
    case 451:
    case "GE_RadioIconEventId":
      return ECsgoGameEvents.GE_RadioIconEventId;
    case 452:
    case "GE_FireBulletsId":
      return ECsgoGameEvents.GE_FireBulletsId;
    case 453:
    case "GE_PlayerBulletHitId":
      return ECsgoGameEvents.GE_PlayerBulletHitId;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ECsgoGameEvents.UNRECOGNIZED;
  }
}

export function eCsgoGameEventsToJSON(object: ECsgoGameEvents): string {
  switch (object) {
    case ECsgoGameEvents.GE_PlayerAnimEventId:
      return "GE_PlayerAnimEventId";
    case ECsgoGameEvents.GE_RadioIconEventId:
      return "GE_RadioIconEventId";
    case ECsgoGameEvents.GE_FireBulletsId:
      return "GE_FireBulletsId";
    case ECsgoGameEvents.GE_PlayerBulletHitId:
      return "GE_PlayerBulletHitId";
    case ECsgoGameEvents.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface CMsgTEPlayerAnimEvent {
  player?: number | undefined;
  event?: number | undefined;
  data?: number | undefined;
}

export interface CMsgTERadioIcon {
  player?: number | undefined;
}

export interface CMsgTEFireBullets {
  origin?: CMsgVector | undefined;
  angles?: CMsgQAngle | undefined;
  weapon_id?: number | undefined;
  mode?: number | undefined;
  seed?: number | undefined;
  player?: number | undefined;
  inaccuracy?: number | undefined;
  recoil_index?: number | undefined;
  spread?: number | undefined;
  sound_type?: number | undefined;
  item_def_index?: number | undefined;
  sound_dsp_effect?: number | undefined;
  ent_origin?: CMsgVector | undefined;
  num_bullets_remaining?: number | undefined;
  attack_type?: number | undefined;
  player_inair?: boolean | undefined;
  player_scoped?: boolean | undefined;
  tick?: number | undefined;
  extra?: CMsgTEFireBullets_Extra | undefined;
}

export interface CMsgTEFireBullets_Extra {
  aim_punch?: CMsgQAngle | undefined;
  attack_tick_count?: number | undefined;
  attack_tick_frac?: number | undefined;
  render_tick_count?: number | undefined;
  render_tick_frac?: number | undefined;
  inaccuracy_move?: number | undefined;
  inaccuracy_air?: number | undefined;
  type?: number | undefined;
}

export interface CMsgPlayerBulletHit {
  attacker_slot?: number | undefined;
  victim_slot?: number | undefined;
  victim_pos?: CMsgVector | undefined;
  hit_group?: number | undefined;
  damage?: number | undefined;
  penetration_count?: number | undefined;
  is_kill?: boolean | undefined;
  through_smoke?: boolean | undefined;
}

function createBaseCMsgTEPlayerAnimEvent(): CMsgTEPlayerAnimEvent {
  return { player: undefined, event: undefined, data: undefined };
}

export const CMsgTEPlayerAnimEvent: MessageFns<CMsgTEPlayerAnimEvent> = {
  encode(message: CMsgTEPlayerAnimEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.player !== undefined) {
      writer.uint32(13).fixed32(message.player);
    }
    if (message.event !== undefined) {
      writer.uint32(16).uint32(message.event);
    }
    if (message.data !== undefined) {
      writer.uint32(24).int32(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CMsgTEPlayerAnimEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCMsgTEPlayerAnimEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.player = reader.fixed32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.event = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.data = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CMsgTEPlayerAnimEvent {
    return {
      player: isSet(object.player) ? globalThis.Number(object.player) : undefined,
      event: isSet(object.event) ? globalThis.Number(object.event) : undefined,
      data: isSet(object.data) ? globalThis.Number(object.data) : undefined,
    };
  },

  toJSON(message: CMsgTEPlayerAnimEvent): unknown {
    const obj: any = {};
    if (message.player !== undefined) {
      obj.player = Math.round(message.player);
    }
    if (message.event !== undefined) {
      obj.event = Math.round(message.event);
    }
    if (message.data !== undefined) {
      obj.data = Math.round(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CMsgTEPlayerAnimEvent>, I>>(base?: I): CMsgTEPlayerAnimEvent {
    return CMsgTEPlayerAnimEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CMsgTEPlayerAnimEvent>, I>>(object: I): CMsgTEPlayerAnimEvent {
    const message = createBaseCMsgTEPlayerAnimEvent();
    message.player = object.player ?? undefined;
    message.event = object.event ?? undefined;
    message.data = object.data ?? undefined;
    return message;
  },
};

function createBaseCMsgTERadioIcon(): CMsgTERadioIcon {
  return { player: undefined };
}

export const CMsgTERadioIcon: MessageFns<CMsgTERadioIcon> = {
  encode(message: CMsgTERadioIcon, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.player !== undefined) {
      writer.uint32(13).fixed32(message.player);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CMsgTERadioIcon {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCMsgTERadioIcon();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.player = reader.fixed32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CMsgTERadioIcon {
    return { player: isSet(object.player) ? globalThis.Number(object.player) : undefined };
  },

  toJSON(message: CMsgTERadioIcon): unknown {
    const obj: any = {};
    if (message.player !== undefined) {
      obj.player = Math.round(message.player);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CMsgTERadioIcon>, I>>(base?: I): CMsgTERadioIcon {
    return CMsgTERadioIcon.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CMsgTERadioIcon>, I>>(object: I): CMsgTERadioIcon {
    const message = createBaseCMsgTERadioIcon();
    message.player = object.player ?? undefined;
    return message;
  },
};

function createBaseCMsgTEFireBullets(): CMsgTEFireBullets {
  return {
    origin: undefined,
    angles: undefined,
    weapon_id: undefined,
    mode: undefined,
    seed: undefined,
    player: undefined,
    inaccuracy: undefined,
    recoil_index: undefined,
    spread: undefined,
    sound_type: undefined,
    item_def_index: undefined,
    sound_dsp_effect: undefined,
    ent_origin: undefined,
    num_bullets_remaining: undefined,
    attack_type: undefined,
    player_inair: undefined,
    player_scoped: undefined,
    tick: undefined,
    extra: undefined,
  };
}

export const CMsgTEFireBullets: MessageFns<CMsgTEFireBullets> = {
  encode(message: CMsgTEFireBullets, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.origin !== undefined) {
      CMsgVector.encode(message.origin, writer.uint32(10).fork()).join();
    }
    if (message.angles !== undefined) {
      CMsgQAngle.encode(message.angles, writer.uint32(18).fork()).join();
    }
    if (message.weapon_id !== undefined) {
      writer.uint32(24).uint32(message.weapon_id);
    }
    if (message.mode !== undefined) {
      writer.uint32(32).uint32(message.mode);
    }
    if (message.seed !== undefined) {
      writer.uint32(40).uint32(message.seed);
    }
    if (message.player !== undefined) {
      writer.uint32(53).fixed32(message.player);
    }
    if (message.inaccuracy !== undefined) {
      writer.uint32(61).float(message.inaccuracy);
    }
    if (message.recoil_index !== undefined) {
      writer.uint32(69).float(message.recoil_index);
    }
    if (message.spread !== undefined) {
      writer.uint32(77).float(message.spread);
    }
    if (message.sound_type !== undefined) {
      writer.uint32(80).int32(message.sound_type);
    }
    if (message.item_def_index !== undefined) {
      writer.uint32(88).uint32(message.item_def_index);
    }
    if (message.sound_dsp_effect !== undefined) {
      writer.uint32(101).fixed32(message.sound_dsp_effect);
    }
    if (message.ent_origin !== undefined) {
      CMsgVector.encode(message.ent_origin, writer.uint32(106).fork()).join();
    }
    if (message.num_bullets_remaining !== undefined) {
      writer.uint32(112).uint32(message.num_bullets_remaining);
    }
    if (message.attack_type !== undefined) {
      writer.uint32(120).uint32(message.attack_type);
    }
    if (message.player_inair !== undefined) {
      writer.uint32(136).bool(message.player_inair);
    }
    if (message.player_scoped !== undefined) {
      writer.uint32(144).bool(message.player_scoped);
    }
    if (message.tick !== undefined) {
      writer.uint32(152).int32(message.tick);
    }
    if (message.extra !== undefined) {
      CMsgTEFireBullets_Extra.encode(message.extra, writer.uint32(130).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CMsgTEFireBullets {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCMsgTEFireBullets();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.origin = CMsgVector.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.angles = CMsgQAngle.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.weapon_id = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.mode = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.seed = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.player = reader.fixed32();
          continue;
        }
        case 7: {
          if (tag !== 61) {
            break;
          }

          message.inaccuracy = reader.float();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.recoil_index = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.spread = reader.float();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.sound_type = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.item_def_index = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 101) {
            break;
          }

          message.sound_dsp_effect = reader.fixed32();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.ent_origin = CMsgVector.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.num_bullets_remaining = reader.uint32();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.attack_type = reader.uint32();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.player_inair = reader.bool();
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.player_scoped = reader.bool();
          continue;
        }
        case 19: {
          if (tag !== 152) {
            break;
          }

          message.tick = reader.int32();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.extra = CMsgTEFireBullets_Extra.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CMsgTEFireBullets {
    return {
      origin: isSet(object.origin) ? CMsgVector.fromJSON(object.origin) : undefined,
      angles: isSet(object.angles) ? CMsgQAngle.fromJSON(object.angles) : undefined,
      weapon_id: isSet(object.weaponId)
        ? globalThis.Number(object.weaponId)
        : isSet(object.weapon_id)
        ? globalThis.Number(object.weapon_id)
        : undefined,
      mode: isSet(object.mode) ? globalThis.Number(object.mode) : undefined,
      seed: isSet(object.seed) ? globalThis.Number(object.seed) : undefined,
      player: isSet(object.player) ? globalThis.Number(object.player) : undefined,
      inaccuracy: isSet(object.inaccuracy) ? globalThis.Number(object.inaccuracy) : undefined,
      recoil_index: isSet(object.recoilIndex)
        ? globalThis.Number(object.recoilIndex)
        : isSet(object.recoil_index)
        ? globalThis.Number(object.recoil_index)
        : undefined,
      spread: isSet(object.spread) ? globalThis.Number(object.spread) : undefined,
      sound_type: isSet(object.soundType)
        ? globalThis.Number(object.soundType)
        : isSet(object.sound_type)
        ? globalThis.Number(object.sound_type)
        : undefined,
      item_def_index: isSet(object.itemDefIndex)
        ? globalThis.Number(object.itemDefIndex)
        : isSet(object.item_def_index)
        ? globalThis.Number(object.item_def_index)
        : undefined,
      sound_dsp_effect: isSet(object.soundDspEffect)
        ? globalThis.Number(object.soundDspEffect)
        : isSet(object.sound_dsp_effect)
        ? globalThis.Number(object.sound_dsp_effect)
        : undefined,
      ent_origin: isSet(object.entOrigin)
        ? CMsgVector.fromJSON(object.entOrigin)
        : isSet(object.ent_origin)
        ? CMsgVector.fromJSON(object.ent_origin)
        : undefined,
      num_bullets_remaining: isSet(object.numBulletsRemaining)
        ? globalThis.Number(object.numBulletsRemaining)
        : isSet(object.num_bullets_remaining)
        ? globalThis.Number(object.num_bullets_remaining)
        : undefined,
      attack_type: isSet(object.attackType)
        ? globalThis.Number(object.attackType)
        : isSet(object.attack_type)
        ? globalThis.Number(object.attack_type)
        : undefined,
      player_inair: isSet(object.playerInair)
        ? globalThis.Boolean(object.playerInair)
        : isSet(object.player_inair)
        ? globalThis.Boolean(object.player_inair)
        : undefined,
      player_scoped: isSet(object.playerScoped)
        ? globalThis.Boolean(object.playerScoped)
        : isSet(object.player_scoped)
        ? globalThis.Boolean(object.player_scoped)
        : undefined,
      tick: isSet(object.tick) ? globalThis.Number(object.tick) : undefined,
      extra: isSet(object.extra) ? CMsgTEFireBullets_Extra.fromJSON(object.extra) : undefined,
    };
  },

  toJSON(message: CMsgTEFireBullets): unknown {
    const obj: any = {};
    if (message.origin !== undefined) {
      obj.origin = CMsgVector.toJSON(message.origin);
    }
    if (message.angles !== undefined) {
      obj.angles = CMsgQAngle.toJSON(message.angles);
    }
    if (message.weapon_id !== undefined) {
      obj.weaponId = Math.round(message.weapon_id);
    }
    if (message.mode !== undefined) {
      obj.mode = Math.round(message.mode);
    }
    if (message.seed !== undefined) {
      obj.seed = Math.round(message.seed);
    }
    if (message.player !== undefined) {
      obj.player = Math.round(message.player);
    }
    if (message.inaccuracy !== undefined) {
      obj.inaccuracy = message.inaccuracy;
    }
    if (message.recoil_index !== undefined) {
      obj.recoilIndex = message.recoil_index;
    }
    if (message.spread !== undefined) {
      obj.spread = message.spread;
    }
    if (message.sound_type !== undefined) {
      obj.soundType = Math.round(message.sound_type);
    }
    if (message.item_def_index !== undefined) {
      obj.itemDefIndex = Math.round(message.item_def_index);
    }
    if (message.sound_dsp_effect !== undefined) {
      obj.soundDspEffect = Math.round(message.sound_dsp_effect);
    }
    if (message.ent_origin !== undefined) {
      obj.entOrigin = CMsgVector.toJSON(message.ent_origin);
    }
    if (message.num_bullets_remaining !== undefined) {
      obj.numBulletsRemaining = Math.round(message.num_bullets_remaining);
    }
    if (message.attack_type !== undefined) {
      obj.attackType = Math.round(message.attack_type);
    }
    if (message.player_inair !== undefined) {
      obj.playerInair = message.player_inair;
    }
    if (message.player_scoped !== undefined) {
      obj.playerScoped = message.player_scoped;
    }
    if (message.tick !== undefined) {
      obj.tick = Math.round(message.tick);
    }
    if (message.extra !== undefined) {
      obj.extra = CMsgTEFireBullets_Extra.toJSON(message.extra);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CMsgTEFireBullets>, I>>(base?: I): CMsgTEFireBullets {
    return CMsgTEFireBullets.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CMsgTEFireBullets>, I>>(object: I): CMsgTEFireBullets {
    const message = createBaseCMsgTEFireBullets();
    message.origin = (object.origin !== undefined && object.origin !== null)
      ? CMsgVector.fromPartial(object.origin)
      : undefined;
    message.angles = (object.angles !== undefined && object.angles !== null)
      ? CMsgQAngle.fromPartial(object.angles)
      : undefined;
    message.weapon_id = object.weapon_id ?? undefined;
    message.mode = object.mode ?? undefined;
    message.seed = object.seed ?? undefined;
    message.player = object.player ?? undefined;
    message.inaccuracy = object.inaccuracy ?? undefined;
    message.recoil_index = object.recoil_index ?? undefined;
    message.spread = object.spread ?? undefined;
    message.sound_type = object.sound_type ?? undefined;
    message.item_def_index = object.item_def_index ?? undefined;
    message.sound_dsp_effect = object.sound_dsp_effect ?? undefined;
    message.ent_origin = (object.ent_origin !== undefined && object.ent_origin !== null)
      ? CMsgVector.fromPartial(object.ent_origin)
      : undefined;
    message.num_bullets_remaining = object.num_bullets_remaining ?? undefined;
    message.attack_type = object.attack_type ?? undefined;
    message.player_inair = object.player_inair ?? undefined;
    message.player_scoped = object.player_scoped ?? undefined;
    message.tick = object.tick ?? undefined;
    message.extra = (object.extra !== undefined && object.extra !== null)
      ? CMsgTEFireBullets_Extra.fromPartial(object.extra)
      : undefined;
    return message;
  },
};

function createBaseCMsgTEFireBullets_Extra(): CMsgTEFireBullets_Extra {
  return {
    aim_punch: undefined,
    attack_tick_count: undefined,
    attack_tick_frac: undefined,
    render_tick_count: undefined,
    render_tick_frac: undefined,
    inaccuracy_move: undefined,
    inaccuracy_air: undefined,
    type: undefined,
  };
}

export const CMsgTEFireBullets_Extra: MessageFns<CMsgTEFireBullets_Extra> = {
  encode(message: CMsgTEFireBullets_Extra, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.aim_punch !== undefined) {
      CMsgQAngle.encode(message.aim_punch, writer.uint32(10).fork()).join();
    }
    if (message.attack_tick_count !== undefined) {
      writer.uint32(16).int32(message.attack_tick_count);
    }
    if (message.attack_tick_frac !== undefined) {
      writer.uint32(29).float(message.attack_tick_frac);
    }
    if (message.render_tick_count !== undefined) {
      writer.uint32(32).int32(message.render_tick_count);
    }
    if (message.render_tick_frac !== undefined) {
      writer.uint32(45).float(message.render_tick_frac);
    }
    if (message.inaccuracy_move !== undefined) {
      writer.uint32(53).float(message.inaccuracy_move);
    }
    if (message.inaccuracy_air !== undefined) {
      writer.uint32(61).float(message.inaccuracy_air);
    }
    if (message.type !== undefined) {
      writer.uint32(64).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CMsgTEFireBullets_Extra {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCMsgTEFireBullets_Extra();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.aim_punch = CMsgQAngle.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.attack_tick_count = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.attack_tick_frac = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.render_tick_count = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.render_tick_frac = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.inaccuracy_move = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 61) {
            break;
          }

          message.inaccuracy_air = reader.float();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.type = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CMsgTEFireBullets_Extra {
    return {
      aim_punch: isSet(object.aimPunch)
        ? CMsgQAngle.fromJSON(object.aimPunch)
        : isSet(object.aim_punch)
        ? CMsgQAngle.fromJSON(object.aim_punch)
        : undefined,
      attack_tick_count: isSet(object.attackTickCount)
        ? globalThis.Number(object.attackTickCount)
        : isSet(object.attack_tick_count)
        ? globalThis.Number(object.attack_tick_count)
        : undefined,
      attack_tick_frac: isSet(object.attackTickFrac)
        ? globalThis.Number(object.attackTickFrac)
        : isSet(object.attack_tick_frac)
        ? globalThis.Number(object.attack_tick_frac)
        : undefined,
      render_tick_count: isSet(object.renderTickCount)
        ? globalThis.Number(object.renderTickCount)
        : isSet(object.render_tick_count)
        ? globalThis.Number(object.render_tick_count)
        : undefined,
      render_tick_frac: isSet(object.renderTickFrac)
        ? globalThis.Number(object.renderTickFrac)
        : isSet(object.render_tick_frac)
        ? globalThis.Number(object.render_tick_frac)
        : undefined,
      inaccuracy_move: isSet(object.inaccuracyMove)
        ? globalThis.Number(object.inaccuracyMove)
        : isSet(object.inaccuracy_move)
        ? globalThis.Number(object.inaccuracy_move)
        : undefined,
      inaccuracy_air: isSet(object.inaccuracyAir)
        ? globalThis.Number(object.inaccuracyAir)
        : isSet(object.inaccuracy_air)
        ? globalThis.Number(object.inaccuracy_air)
        : undefined,
      type: isSet(object.type) ? globalThis.Number(object.type) : undefined,
    };
  },

  toJSON(message: CMsgTEFireBullets_Extra): unknown {
    const obj: any = {};
    if (message.aim_punch !== undefined) {
      obj.aimPunch = CMsgQAngle.toJSON(message.aim_punch);
    }
    if (message.attack_tick_count !== undefined) {
      obj.attackTickCount = Math.round(message.attack_tick_count);
    }
    if (message.attack_tick_frac !== undefined) {
      obj.attackTickFrac = message.attack_tick_frac;
    }
    if (message.render_tick_count !== undefined) {
      obj.renderTickCount = Math.round(message.render_tick_count);
    }
    if (message.render_tick_frac !== undefined) {
      obj.renderTickFrac = message.render_tick_frac;
    }
    if (message.inaccuracy_move !== undefined) {
      obj.inaccuracyMove = message.inaccuracy_move;
    }
    if (message.inaccuracy_air !== undefined) {
      obj.inaccuracyAir = message.inaccuracy_air;
    }
    if (message.type !== undefined) {
      obj.type = Math.round(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CMsgTEFireBullets_Extra>, I>>(base?: I): CMsgTEFireBullets_Extra {
    return CMsgTEFireBullets_Extra.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CMsgTEFireBullets_Extra>, I>>(object: I): CMsgTEFireBullets_Extra {
    const message = createBaseCMsgTEFireBullets_Extra();
    message.aim_punch = (object.aim_punch !== undefined && object.aim_punch !== null)
      ? CMsgQAngle.fromPartial(object.aim_punch)
      : undefined;
    message.attack_tick_count = object.attack_tick_count ?? undefined;
    message.attack_tick_frac = object.attack_tick_frac ?? undefined;
    message.render_tick_count = object.render_tick_count ?? undefined;
    message.render_tick_frac = object.render_tick_frac ?? undefined;
    message.inaccuracy_move = object.inaccuracy_move ?? undefined;
    message.inaccuracy_air = object.inaccuracy_air ?? undefined;
    message.type = object.type ?? undefined;
    return message;
  },
};

function createBaseCMsgPlayerBulletHit(): CMsgPlayerBulletHit {
  return {
    attacker_slot: undefined,
    victim_slot: undefined,
    victim_pos: undefined,
    hit_group: undefined,
    damage: undefined,
    penetration_count: undefined,
    is_kill: undefined,
    through_smoke: undefined,
  };
}

export const CMsgPlayerBulletHit: MessageFns<CMsgPlayerBulletHit> = {
  encode(message: CMsgPlayerBulletHit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attacker_slot !== undefined) {
      writer.uint32(8).int32(message.attacker_slot);
    }
    if (message.victim_slot !== undefined) {
      writer.uint32(16).int32(message.victim_slot);
    }
    if (message.victim_pos !== undefined) {
      CMsgVector.encode(message.victim_pos, writer.uint32(26).fork()).join();
    }
    if (message.hit_group !== undefined) {
      writer.uint32(32).int32(message.hit_group);
    }
    if (message.damage !== undefined) {
      writer.uint32(40).int32(message.damage);
    }
    if (message.penetration_count !== undefined) {
      writer.uint32(48).int32(message.penetration_count);
    }
    if (message.is_kill !== undefined) {
      writer.uint32(56).bool(message.is_kill);
    }
    if (message.through_smoke !== undefined) {
      writer.uint32(64).bool(message.through_smoke);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CMsgPlayerBulletHit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCMsgPlayerBulletHit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.attacker_slot = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.victim_slot = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.victim_pos = CMsgVector.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.hit_group = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.damage = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.penetration_count = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.is_kill = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.through_smoke = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CMsgPlayerBulletHit {
    return {
      attacker_slot: isSet(object.attackerSlot)
        ? globalThis.Number(object.attackerSlot)
        : isSet(object.attacker_slot)
        ? globalThis.Number(object.attacker_slot)
        : undefined,
      victim_slot: isSet(object.victimSlot)
        ? globalThis.Number(object.victimSlot)
        : isSet(object.victim_slot)
        ? globalThis.Number(object.victim_slot)
        : undefined,
      victim_pos: isSet(object.victimPos)
        ? CMsgVector.fromJSON(object.victimPos)
        : isSet(object.victim_pos)
        ? CMsgVector.fromJSON(object.victim_pos)
        : undefined,
      hit_group: isSet(object.hitGroup)
        ? globalThis.Number(object.hitGroup)
        : isSet(object.hit_group)
        ? globalThis.Number(object.hit_group)
        : undefined,
      damage: isSet(object.damage) ? globalThis.Number(object.damage) : undefined,
      penetration_count: isSet(object.penetrationCount)
        ? globalThis.Number(object.penetrationCount)
        : isSet(object.penetration_count)
        ? globalThis.Number(object.penetration_count)
        : undefined,
      is_kill: isSet(object.isKill)
        ? globalThis.Boolean(object.isKill)
        : isSet(object.is_kill)
        ? globalThis.Boolean(object.is_kill)
        : undefined,
      through_smoke: isSet(object.throughSmoke)
        ? globalThis.Boolean(object.throughSmoke)
        : isSet(object.through_smoke)
        ? globalThis.Boolean(object.through_smoke)
        : undefined,
    };
  },

  toJSON(message: CMsgPlayerBulletHit): unknown {
    const obj: any = {};
    if (message.attacker_slot !== undefined) {
      obj.attackerSlot = Math.round(message.attacker_slot);
    }
    if (message.victim_slot !== undefined) {
      obj.victimSlot = Math.round(message.victim_slot);
    }
    if (message.victim_pos !== undefined) {
      obj.victimPos = CMsgVector.toJSON(message.victim_pos);
    }
    if (message.hit_group !== undefined) {
      obj.hitGroup = Math.round(message.hit_group);
    }
    if (message.damage !== undefined) {
      obj.damage = Math.round(message.damage);
    }
    if (message.penetration_count !== undefined) {
      obj.penetrationCount = Math.round(message.penetration_count);
    }
    if (message.is_kill !== undefined) {
      obj.isKill = message.is_kill;
    }
    if (message.through_smoke !== undefined) {
      obj.throughSmoke = message.through_smoke;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CMsgPlayerBulletHit>, I>>(base?: I): CMsgPlayerBulletHit {
    return CMsgPlayerBulletHit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CMsgPlayerBulletHit>, I>>(object: I): CMsgPlayerBulletHit {
    const message = createBaseCMsgPlayerBulletHit();
    message.attacker_slot = object.attacker_slot ?? undefined;
    message.victim_slot = object.victim_slot ?? undefined;
    message.victim_pos = (object.victim_pos !== undefined && object.victim_pos !== null)
      ? CMsgVector.fromPartial(object.victim_pos)
      : undefined;
    message.hit_group = object.hit_group ?? undefined;
    message.damage = object.damage ?? undefined;
    message.penetration_count = object.penetration_count ?? undefined;
    message.is_kill = object.is_kill ?? undefined;
    message.through_smoke = object.through_smoke ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
